using System;
using System.Collections.Generic;
using System.Text;

namespace TmpSandBox.TstSmartObj
{
    public class LogicalObject: AbstractLogicalObject
    {
        /*public static bool operator == (LogicalObject item1, ILogicalObject item2)
        {
            NLog.LogManager.GetCurrentClassLogger().Info("==");

            return true;
        }

        public static bool operator != (LogicalObject item1, ILogicalObject item2)
        {
            NLog.LogManager.GetCurrentClassLogger().Info("!=");

            return !(item1 == item2);
        }*/
    }
}
