npc PeaceKeeper
{
    on Init =>
    {
		"Begin" >> @>log;
        "The Beatles!" >> @>log;
		//@@host.`go`(to: #@[-45, 20]);
		@@host.`go`(to: #@[90, 10]);
		//@@host.`go`(to: #@[-20, 10]);
		//@@host.`go`(to: #@[-10, 10]);
		//@@host.`go`(to: #@[-20, 5]);
		//@@host.`go`(to: #@[2, 10]);
	    "End" >> @>log;
    }
}
